/* 20. Дано целое число N (>0). Используя один цикл, найти сумму 
1!+2!+3!+...+N! 
Чтобы избежать целочисленного переполнения, вычислять это произведение с помощью 
вещественной переменной и вывести его как вещественное число.*/

//Начал: 19:11 закончил:19:20

#include <stdio.h>

int main()
{
	int N = 8;
	double factorial = 1.0;
	double sum = 0.0;
	for (int i=1; i<=N; i++)
	{
		factorial *= ((double)i);
		sum += factorial; 
	
	}
printf("Sum = %f", sum);

return 0;
}
